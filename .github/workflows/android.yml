name: Android CI
permissions:
  contents: read

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Set up JDK 8
  uses: actions/setup-java@v5
  with:
    distribution: 'temurin'
    java-version: '8'
